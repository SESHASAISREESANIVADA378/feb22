#include<stdio.h>
main()
{
	int i,n,osum=0,esum=0;
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	{
		if(i%2==0)
    	esum=esum+i;
    	else
    	osum=osum+i;
	}
	printf("odd sum=%d",osum);
	printf("even sum=%d",esum);
}
